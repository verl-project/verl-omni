# Format checks enforced on CI:
# 1. Comments must appear above each field.
# 2. There must be a blank line between each field.
# 3. Inline comments (after a field on the same line) are not allowed.
# 4. Indentation level is respected for nested fields.
#
# Per-role profiler config, mirroring `verl/trainer/config/profiler/profiler.yaml`
# from upstream verl, including NPU support for Ascend profiling.

# Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
_target_: verl.utils.profiler.ProfilerConfig

# profiler tool, default same as profiler.tool in global config.
# Supported values in verl-omni: nsys, npu, torch, torch_memory.
tool: torch

# whether to enable profiling for this role
enable: False

# Whether to profile all ranks.
all_ranks: False

# The ranks that will be profiled. [] or [0,1,...]
ranks: []

# profile results saving path
save_path: ${oc.select:global_profiler.save_path,outputs/profile}

# Tool-specific configuration. The active block is selected by `tool` above.
tool_config:
  npu:
    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.utils.profiler.config.NPUToolConfig

    # Contents to profile, can be empty
    # options: npu, cpu, memory, shapes, module, stack
    contents: [ ]

    # Collection level, optional values: level_none, level0, level1, level2.
    level: "level0"

    # Whether to automatically parse the data.
    analysis: True

    # True for each task has its own database, False for all tasks in one training step share one database.
    discrete: False

    # Start collecting profiler data from this response-token index.
    # null means collect from the first response token (full collection start).
    profile_token_start: null

    # Stop collecting profiler data at this response-token index (exclusive).
    # null means collect until the end (full collection).
    profile_token_end: null

    name: npu


  # NVIDIA Nsight Systems
  nsys:

    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.utils.profiler.config.NsightToolConfig

    # True for each task has its own database, False for all tasks in one training step share one database.
    discrete: ${oc.select:global_profiler.global_tool_config.nsys.discrete,false}

    name: nsight

  # PyTorch Profiler (torch.profiler)
  torch:

    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.utils.profiler.config.TorchProfilerToolConfig

    # Contents to profile, can be empty.
    # options: cuda, cpu, memory, shapes, stack
    contents: []

    # True for each task has its own database, False for all tasks in one training step share one database.
    discrete: false

    name: torch

  # Torch CUDA memory snapshot dumping
  torch_memory:

    # Required when using verl.utils.omega_conf_to_dataclass to instantiate dataclass configs
    _target_: verl.utils.profiler.config.TorchMemoryToolConfig

    # Maximum number of memory allocation entries to track
    trace_alloc_max_entries: ${oc.select:global_profiler.global_tool_config.torch_memory.trace_alloc_max_entries,100000}

    # Stack trace depth for memory allocations
    stack_depth: ${oc.select:global_profiler.global_tool_config.torch_memory.stack_depth,32}

    name: torch_memory
