name: upstream-sync

on:
  schedule:
    - cron: '0 2 * * 1'  # weekly Monday 2 AM UTC
  workflow_dispatch:      # manual trigger for testing / backfill
  push:
    branches: [gitaction]  # TEMP: remove before merging to main

concurrency:
  group: upstream-sync
  cancel-in-progress: false  # never cancel a running sync mid-flight

permissions:
  contents: write       # create branches for auto-PRs (never pushes to main)
  pull-requests: write  # open PRs
  actions: write        # read/write SHA state variables

jobs:
  signature-drift:
    name: "Signature Drift — API compatibility"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix:
        python-version: ["3.10"]

    steps:
      # -----------------------------------------------------------------------
      # 1. Setup
      # -----------------------------------------------------------------------
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          # Fetch full history so the bot can create branches cleanly
          fetch-depth: 0

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install upstream dependencies
        run: |
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install --no-deps "verl @ git+https://github.com/verl-project/verl.git@main"
          pip install "git+https://github.com/vllm-project/vllm-omni.git@main"
          pip install -r requirements.txt
          pip install -r requirements-test.txt
          pip install --no-deps -e .

      # -----------------------------------------------------------------------
      # 2. Detect signature drift
      # -----------------------------------------------------------------------
      - name: Detect signature drift
        id: detect
        run: |
          python scripts/upstream_sync/check_upstream_signatures.py
          EXIT=$?
          if [ $EXIT -eq 2 ]; then
            echo "::error::Upstream packages not resolvable — check pip install step"
            exit 1
          fi
          # EXIT 0 = no drift, EXIT 1 = drift found; both are workflow successes here
          exit 0

      # -----------------------------------------------------------------------
      # 3. Classify changes and prepare AI agent input
      # -----------------------------------------------------------------------
      - name: Classify changes and prepare AI agent input
        if: steps.detect.outputs.has_changes == 'true'
        id: fix
        run: python scripts/upstream_sync/apply_signature_fixes.py

      # -----------------------------------------------------------------------
      # 4. AI agent for signature changes
      #    Runs only when apply_signature_fixes.py found cases to route to the AI agent
      #    (param_removal, others).
      #    AI agent edits are added to the SAME PR as non-breaking and rename notes.
      # -----------------------------------------------------------------------
      - name: Generate AI agent prompt batches
        if: steps.fix.outputs.has_ai_work == 'true'
        run: python scripts/upstream_sync/generate_signature_drift_prompt.py

      - name: Install AI agent CLI
        if: steps.fix.outputs.has_ai_work == 'true'
        run: |
          curl https://cursor.com/install -fsS | bash
          echo "$HOME/.cursor/bin" >> "$GITHUB_PATH"

      - name: Run AI agent on signature drift changes
        if: steps.fix.outputs.has_ai_work == 'true'
        id: ai_sig
        # Restricted: AI agent may read anything, write verl_omni Python files, and
        # write per-batch edit summary files. Git operations stay in workflow steps.
        run: |
          for f in signature_drift_ai_prompt_batch*.md; do
            [ -f "$f" ] || continue
            cursor agent \
              -p "$(cat "$f")" \
              --allow "Read(**)" \
              --allow "Write(verl_omni/**/*.py)" \
              --allow "Write(ai_edits_summary_batch*.md)" \
              --deny  "Shell(git)" \
              --deny  "Shell(gh)" \
              --deny  "Write(.github/**)" \
              --deny  "Write(scripts/**)" \
              --deny  "Write(requirements*.txt)" \
              --deny  "Write(setup.py)"
          done
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}

      - name: Append AI edit summaries to PR body
        if: steps.fix.outputs.has_ai_work == 'true'
        run: |
          python3 - <<'PYEOF'
          import pathlib
          summaries = sorted(pathlib.Path(".").glob("ai_edits_summary_batch*.md"))
          if not summaries:
              print("No edit summary files written by the AI agent.")
          else:
              pr_body = pathlib.Path("pr_body_signature_drift.md")
              with pr_body.open("a") as f:
                  f.write("\n### AI agent edit log\n")
                  for s in summaries:
                      f.write(s.read_text())
                      f.write("\n")
              print(f"Appended {len(summaries)} summary file(s) to PR body.")
          PYEOF

      # -----------------------------------------------------------------------
      # 5. Run sanity checks on all edited code
      # -----------------------------------------------------------------------
      - name: Sanity check edited code
        if: steps.fix.outputs.has_ai_work == 'true'
        id: sanity
        continue-on-error: true  # failure adds a label but doesn't block PR creation
        run: pytest -s -x tests/special_sanity

      # -----------------------------------------------------------------------
      # 6. Open auto-PR
      # -----------------------------------------------------------------------
      - name: Configure git identity
        if: steps.fix.outputs.has_changes == 'true'
        run: |
          git config user.name  "upstream-sync-bot"
          git config user.email "upstream-sync-bot@users.noreply.github.com"

      - name: Create branch and open PR
        if: steps.fix.outputs.has_changes == 'true'
        id: pr
        run: |
          BRANCH="bot/upstream-sync-sig-$(date -u +%Y%m%d)"

          # Bail out if branch already exists (idempotency — same day re-run)
          if git ls-remote --exit-code origin "refs/heads/$BRANCH" 2>/dev/null; then
            echo "::notice::Branch $BRANCH already exists — skipping duplicate PR"
            exit 0
          fi

          git checkout -b "$BRANCH"

          # Promote fresh snapshot to committed snapshot
          if [ -f .github/upstream_sync/api_signatures_fresh.json ]; then
            cp .github/upstream_sync/api_signatures_fresh.json .github/upstream_sync/api_signatures.json
          fi

          # Stage only the files we own: the snapshot + any edits to verl_omni/
          git add .github/upstream_sync/api_signatures.json
          git add verl_omni/ || true

          # Don't commit if nothing actually changed on disk
          if git diff --cached --quiet; then
            echo "::notice::No file changes — upstream changed but no verl-omni edits needed"
            exit 0
          fi

          git commit -m "chore(upstream-sync): signature drift $(date -u +%Y-%m-%d)"
          git push origin "$BRANCH"

          LABELS="upstream-sync"
          if [ "${{ steps.ai_sig.outcome }}" = "success" ]; then
            LABELS="$LABELS,ai-assisted"
          fi
          if [ "${{ steps.sanity.outcome }}" = "failure" ]; then
            LABELS="$LABELS,sanity-failed"
          fi

          gh pr create \
            --title "[bot][upstream-sync] Signature drift $(date -u +%Y-%m-%d)" \
            --body-file pr_body_signature_drift.md \
            --label "$LABELS" \
            --base main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # 7. Record upstream SHA for behavioral-drift job
      #    Stores the verl HEAD SHA so the behavioral-drift job can do
      #    `git log <last_sha>..HEAD` without re-processing old commits.
      # -----------------------------------------------------------------------
      - name: Record upstream verl SHA
        if: always()  # update even if no drift was found, so we advance the pointer
        run: |
          # Get the SHA of the verl package we just installed
          VERL_SHA=$(python3 - <<'EOF'
          import importlib.util, subprocess, pathlib
          spec = importlib.util.find_spec("verl")
          if spec:
              pkg_dir = pathlib.Path(spec.origin).parent
              result = subprocess.run(
                  ["git", "rev-parse", "HEAD"],
                  cwd=pkg_dir, capture_output=True, text=True
              )
              print(result.stdout.strip())
          EOF
          )
          if [ -z "$VERL_SHA" ]; then
            echo "::warning::Could not determine verl HEAD SHA — SHA variable not updated"
            exit 0
          fi
          echo "verl SHA: $VERL_SHA"

          # Upsert the repository variable (create if missing, patch if exists)
          gh api "repos/${{ github.repository }}/actions/variables/UPSTREAM_VERL_SHA" \
            --method PATCH \
            -f value="$VERL_SHA" 2>/dev/null || \
          gh api "repos/${{ github.repository }}/actions/variables" \
            --method POST \
            -f name="UPSTREAM_VERL_SHA" \
            -f value="$VERL_SHA"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # 8. Clean up temp files so they don't pollute the workspace
      # -----------------------------------------------------------------------
      - name: Clean up temp files
        if: always()
        run: |
          rm -f signature_changes.json remaining_for_ai.json pr_body_signature_drift.md \
                signature_drift_ai_prompt_batch*.md ai_edits_summary_batch*.md \
                .github/upstream_sync/api_signatures_fresh.json

  # =========================================================================
  # Behavioral Drift — detects changes in upstream files that verl-omni
  # mirrors/extends, then calls the AI agent to propose minimal adaptations.
  # =========================================================================
  behavioral-drift:
    name: "Behavioral Drift — upstream code changes"
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # Run independently of signature-drift — both can open PRs on the same day
    needs: []

    steps:
      # -----------------------------------------------------------------------
      # 1. Setup
      # -----------------------------------------------------------------------
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up Python 3.10
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: "3.10"

      - name: Install minimal Python deps
        run: pip install pyyaml

      # -----------------------------------------------------------------------
      # 2. Read last processed SHAs from repository variables
      # -----------------------------------------------------------------------
      - name: Read upstream SHAs
        id: shas
        run: |
          # Use if/else so the else branch overwrites the variable even when gh api
          # writes a JSON error body to stdout before exiting non-zero.
          # The $(cmd || echo "") pattern does NOT work for this because it captures
          # both the command's stdout AND the echo output.
          if VERL_SHA=$(gh api "repos/${{ github.repository }}/actions/variables/UPSTREAM_VERL_SHA" \
            --jq '.value' 2>/dev/null); then :; else VERL_SHA=""; fi
          if VLLM_OMNI_SHA=$(gh api "repos/${{ github.repository }}/actions/variables/UPSTREAM_VLLM_OMNI_SHA" \
            --jq '.value' 2>/dev/null); then :; else VLLM_OMNI_SHA=""; fi
          echo "UPSTREAM_VERL_SHA=$VERL_SHA"         >> "$GITHUB_ENV"
          echo "UPSTREAM_VLLM_OMNI_SHA=$VLLM_OMNI_SHA" >> "$GITHUB_ENV"
          echo "verl_sha=${VERL_SHA:0:12}"           >> "$GITHUB_OUTPUT"
          echo "vllm_omni_sha=${VLLM_OMNI_SHA:0:12}" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # 3. Detect behavioral drift via GitHub compare API
      # -----------------------------------------------------------------------
      - name: Detect behavioral drift
        id: detect
        run: |
          python scripts/upstream_sync/check_upstream_behavioral.py
          EXIT=$?
          if [ $EXIT -eq 2 ]; then
            echo "::error::GitHub API error in behavioral check"
            exit 1
          fi
          exit 0  # EXIT 0 = no drift, EXIT 1 = drift; both are workflow OK
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # 4. Generate AI agent prompt batches
      # -----------------------------------------------------------------------
      - name: Generate AI agent prompt batches
        if: steps.detect.outputs.has_changes == 'true'
        run: python scripts/upstream_sync/generate_behavioral_drift_prompt.py

      # -----------------------------------------------------------------------
      # 5. Install and run AI agent (restricted autonomy)
      # -----------------------------------------------------------------------
      - name: Install AI agent CLI
        if: steps.detect.outputs.has_changes == 'true'
        run: |
          curl https://cursor.com/install -fsS | bash
          echo "$HOME/.cursor/bin" >> "$GITHUB_PATH"

      - name: Run AI agent on behavioral drift changes
        if: steps.detect.outputs.has_changes == 'true'
        id: ai
        # Restricted autonomy: AI agent may read anything, write verl_omni/*.py, and
        # write per-batch edit summary files. All git/gh ops remain in workflow steps.
        run: |
          for f in behavioral_drift_ai_prompt_batch*.md; do
            [ -f "$f" ] || continue
            cursor agent \
              -p "$(cat "$f")" \
              --allow "Read(**)" \
              --allow "Write(verl_omni/**/*.py)" \
              --allow "Write(behavioral_edits_summary_batch*.md)" \
              --deny  "Shell(git)" \
              --deny  "Shell(gh)" \
              --deny  "Write(.github/**)" \
              --deny  "Write(scripts/**)" \
              --deny  "Write(requirements*.txt)" \
              --deny  "Write(setup.py)"
          done
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}

      # -----------------------------------------------------------------------
      # 6. Sanity check AI agent's edits
      # -----------------------------------------------------------------------
      - name: Sanity check AI agent edits
        if: steps.ai.outcome == 'success'
        id: sanity
        continue-on-error: true
        run: |
          pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
          pip install --no-deps "verl @ git+https://github.com/verl-project/verl.git@main"
          pip install -r requirements.txt --no-deps
          pip install --no-deps -e .
          pytest -s -x tests/special_sanity

      # -----------------------------------------------------------------------
      # 7. Open auto-PR with AI agent's changes
      # -----------------------------------------------------------------------
      - name: Configure git identity
        if: steps.ai.outcome == 'success'
        run: |
          git config user.name  "upstream-sync-bot"
          git config user.email "upstream-sync-bot@users.noreply.github.com"

      - name: Create branch and open PR
        if: steps.ai.outcome == 'success'
        id: pr
        run: |
          BRANCH="bot/upstream-sync-behavioral-$(date -u +%Y%m%d)"

          if git ls-remote --exit-code origin "refs/heads/$BRANCH" 2>/dev/null; then
            echo "::notice::Branch $BRANCH already exists — skipping"
            exit 0
          fi

          # Only proceed if the AI agent actually changed something
          if git diff --quiet; then
            echo "::notice::AI agent made no file changes — no PR needed"
            exit 0
          fi

          git checkout -b "$BRANCH"
          git add verl_omni/
          git commit -m "chore(upstream-sync): behavioral drift $(date -u +%Y-%m-%d)"
          git push origin "$BRANCH"

          LABELS="upstream-sync,upstream-sync-behavioral"
          if [ "${{ steps.sanity.outcome }}" = "failure" ]; then
            LABELS="$LABELS,sanity-failed"
          fi

          # Build PR body from behavioral_changes.json + AI edit summaries
          python3 - <<'PYEOF'
          import json, pathlib
          root = pathlib.Path(".")
          data = json.loads((root / "behavioral_changes.json").read_text())
          changes = data.get("changes", [])
          lines = [
              "## Upstream Sync — Behavioral Drift",
              "",
              "_Auto-generated by the daily upstream-sync bot + AI agent._",
              "",
              "### Upstream changes analyzed",
          ]
          for c in changes:
              e = c["entry"]
              lines.append(f"- `{e['file']}` ({e['upstream_repo']}) — symbols: {e['symbols']}")
          summaries = sorted(root.glob("behavioral_edits_summary_batch*.md"))
          if summaries:
              lines += ["", "### AI agent edit log"]
              for s in summaries:
                  lines.append(s.read_text())
          lines += [
              "",
              "### Review checklist",
              "- [ ] Read all AI-generated edits line by line",
              "- [ ] Run the full test suite on GPU if training-path code changed",
              "- [ ] Verify diffusion semantics are preserved (not just LLM logic copied)",
              "- [ ] Check `tests/special_sanity` passes",
          ]
          pathlib.Path("pr_body_behavioral_drift.md").write_text("\n".join(lines) + "\n")
          PYEOF

          gh pr create \
            --title "[bot][upstream-sync] Behavioral drift $(date -u +%Y-%m-%d)" \
            --body-file pr_body_behavioral_drift.md \
            --label "$LABELS" \
            --base main
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # 8. Update SHA variables to current upstream HEAD
      #    Only update after a successful run (detect + no API errors).
      #    If the AI agent or PR creation fails, SHA is NOT updated so the next run
      #    re-processes the same upstream changes.
      # -----------------------------------------------------------------------
      - name: Update upstream SHA variables
        if: steps.detect.outcome == 'success'
        run: |
          if [ ! -f upstream_current_shas.json ]; then
            echo "::notice::No current SHAs file found — variables not updated"
            exit 0
          fi

          python3 - <<'PYEOF'
          import json, os, subprocess, sys

          shas = json.loads(open("upstream_current_shas.json").read())
          repo = os.environ["GITHUB_REPOSITORY"]
          token = os.environ["GH_TOKEN"]

          VAR_MAP = {
              "verl-project/verl":        "UPSTREAM_VERL_SHA",
              "vllm-project/vllm-omni":   "UPSTREAM_VLLM_OMNI_SHA",
          }

          for upstream_repo, sha in shas.items():
              var_name = VAR_MAP.get(upstream_repo)
              if not var_name or not sha:
                  continue
              for method in ("PATCH", "POST"):
                  r = subprocess.run(
                      ["gh", "api", f"repos/{repo}/actions/variables/{var_name}",
                       "--method", method, "-f", f"value={sha}"],
                      capture_output=True, text=True
                  )
                  if r.returncode == 0:
                      print(f"Updated {var_name} = {sha[:12]}")
                      break
                  if "404" not in r.stderr:
                      print(f"ERROR updating {var_name}: {r.stderr}", file=sys.stderr)
                      break
          PYEOF
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # -----------------------------------------------------------------------
      # 9. Clean up temp files
      # -----------------------------------------------------------------------
      - name: Clean up temp files
        if: always()
        run: |
          rm -f behavioral_changes.json behavioral_drift_ai_prompt_batch*.md \
                behavioral_edits_summary_batch*.md \
                pr_body_behavioral_drift.md upstream_current_shas.json
